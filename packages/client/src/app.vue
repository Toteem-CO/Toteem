<template>
  <NuxtLayout>
    <NuxtPage/>
  </NuxtLayout>
</template>

<!--<script setup>
  import '~/assets/styles/tailwind.scss';
  import '~/assets/fonts/inter.scss';
</script>-->

<style lang="scss">
  body {
/*    @apply text-gray-500 bg-gray-50;

    .tooltip {
      @apply opacity-0 absolute bg-gray-700 text-gray-100 invisible;
      transition-property: background-color,border-color,color,fill,stroke,opacity,box-shadow,transform;
      transition-duration: .15s;
      transition-duration: .2s;
      transition-timing-function: cubic-bezier(.4,0,.2,1);
      transition-delay: .1s;
      transform: translateX(-50%);
      border-radius: 0.25rem;
      font-size: .875rem;
      line-height: 1.25rem;
      max-width: 20rem;
      padding: 0.25rem 0.5rem;
      width: -webkit-max-content;
      width: -moz-max-content;
      width: max-content;
    }

    .tooltip:after {
      @apply block absolute border-4 border-transparent border-solid w-0 h-0;
      content: "";
      transform: translateX(-50%);
    }

    .tooltip-top {
      @apply top-[-2rem] left-1/2 right-auto bottom-auto
    }

    .tooltip-top:after {
      @apply border-t-gray-700 top-auto left-1/2 right-auto bottom-[-8px];
    }

    .tooltip-bottom {
      @apply top-auto left-1/2 right-auto bottom-[-2rem]
    }

    .tooltip-bottom:after {
      @apply border-b-gray-700 top-[-8px] left-1/2 right-auto bottom-auto;
    }

    .tooltip-arrow-left:after {
      @apply left-[10%]
    }

    .has-tooltip {
      position: relative;
    }

    .has-tooltip:hover .tooltip {
      @apply opacity-100 z-50 visible;
    }*/
  }
</style>
